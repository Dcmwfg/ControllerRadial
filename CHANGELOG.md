# Changelog

## 1.0.0 — first public release (Minecraft 1.20.1, Forge)

首个开源版本。功能一览：

1. 按住 **D-Pad 右** 打开手柄轮盘，**右摇杆**选择格子（高亮会保持），**摇杆回中**执行；**LB / RB** 翻页。
2. **多套轮盘**：默认 3 套、最多 12 套，可添加 / 删除 / **重命名**；每套最多 12 格。
3. 每格的三种动作：
   - **功能**：任意 `KeyMapping`（原版或模组），支持 **一下 / 定时长按 0.5–5 秒 / 切换保持**；**功能本身没绑键盘键位也能触发**。
   - **聊天命令**：支持 `@p`、WorldEdit 的 `//` 写法、只复制到剪贴板。
   - **跳转轮盘**：选中后不关轮盘，直接换到目标轮盘（对标 MineMenu 的 CATEGORY）。
4. 每格可自定义**显示文字**（自适应缩放）和**物品图标**（可选）。
5. **图形化编辑**：选项 → 控制 → 「轮盘配置」（也可从模组列表进）；功能选择器带搜索与「常用功能」，物品选择器按名字/id 搜索。
6. 轮盘内**直接编辑**：长按 **Y/△** 打开当前轮盘的设置页。
7. **通用手柄鼠标补充**：`界面：拖拽（按住左键）`（默认 RS 按下）——任何界面都能拖拽；Xaero 地图另有 **RT 右键** 与 **方向键微调光标**（默认已绑定）。
8. 未绑键功能的触发、轮盘布局、底部按钮网格等都有单元式自检覆盖（`gradlew smokeTest`，300+ 项）。

依赖：Minecraft 1.20.1 + Forge 47.x + [Controlify（Forge 1.20.1 社区回移版）](https://github.com/echoEllet/controlify) 2.1.9+。

### 已知取舍

1. 默认开启键选 D-Pad 右是为了避开左扳机「使用」的冲突；Controlify 自带的轮盘默认也在 D-Pad 右，两者同键时本 mod 会抢先打开（日志会提示）。
2. Controlify 一个绑定只能对应一个输入，所以「LB+RB 组合」这类手势由本 mod 自己判定。
3. 地图绑定里「左摇杆光标 / 右摇杆缩放」故意不重复绑定，用 Controlify 自带的更好（重复会变成双倍速度）。
4. 未做实机手柄回归测试（开发环境无法接手柄），手感相关的数值（死区、速度、长按阈值）都集中在常量里，便于调整。
